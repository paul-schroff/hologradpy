
.. DO NOT EDIT.
.. THIS FILE WAS AUTOMATICALLY GENERATED BY SPHINX-GALLERY.
.. TO MAKE CHANGES, EDIT THE SOURCE PYTHON FILE:
.. "auto_examples\calibration\camera_mapping\checkerboard_camera_mapping.py"
.. LINE NUMBERS ARE GIVEN BELOW.

.. only:: html

    .. note::
        :class: sphx-glr-download-link-note

        :ref:`Go to the end <sphx_glr_download_auto_examples_calibration_camera_mapping_checkerboard_camera_mapping.py>`
        to download the full example code.

.. rst-class:: sphx-glr-example-title

.. _sphx_glr_auto_examples_calibration_camera_mapping_checkerboard_camera_mapping.py:


Camera mapping from a checkerboard
==================================

Displaying a checkerboard and detecting its corners, fitting an affine transform 
between the image plane and the sensor.

.. GENERATED FROM PYTHON SOURCE LINES 10-37

.. code-block:: Python

    from pathlib import Path

    import matplotlib.pyplot as plt
    import torch

    from hologradpy.hardware import (
        SimulatedSLMTorch,
        SimulatedCameraTorch,
        open_camera,
        open_slm,
    )

    from hologradpy.calibration.camera_mapping import CheckerboardMapper

    from hologradpy.optics.systems import SLMFFT, SLMFFTAffine
    from hologradpy.optics.modules.slm_fields import PixelwiseSLMField
    from hologradpy.optics.modules.virtual_slms import VirtualSLM
    from hologradpy.optics.complex_amplitude import ComplexAmplitude, FieldGeometry

    from hologradpy.profiles.amplitude import gaussian_beam_intensity
    from hologradpy.utils import get_device, gpu_to_numpy

    device = get_device(verbose=True)
    data_path = "../data/"
    Path(data_path).mkdir(exist_ok=True)
    # %matplotlib qt5





.. rst-class:: sphx-glr-script-out

 .. code-block:: none

    Using device: cuda




.. GENERATED FROM PYTHON SOURCE LINES 38-87

.. code-block:: Python

    slm_geometry = FieldGeometry(
        resolution=(1024, 1280),
        pixel_size=torch.tensor([12.5e-6, 12.5e-6], device=device),
        wavelength=torch.tensor(0.630e-6, device=device),
    )

    slm = open_slm(
        SimulatedSLMTorch, input_geometry=slm_geometry, bitdepth=8
    )

    gaussian_intensity = gaussian_beam_intensity(
        *slm.get_spatial_grid(device),
        beam_radius=5e-3,
    )
    gaussian_beam = ComplexAmplitude(
        gaussian_intensity.sqrt() + 0j,
        wavelength=slm_geometry.wavelength,
        pixel_size=slm_geometry.pixel_size,
        power=1e-3,
    )

    simulated_camera_model = SLMFFTAffine(
        input_geometry=slm_geometry,
        virtual_slm=slm.virtual_slm,
        camera_resolution=(960, 1440),
        camera_pixel_size=(3.75e-6, 3.75e-6),
        focal_length=0.25,
        slm_field=PixelwiseSLMField(gaussian_beam),
        padded_resolution=(2048, 2048),
        camera_angle=0,
        camera_shift=(0, 0),
        power_normalized=True,
    )

    camera = open_camera(
        SimulatedCameraTorch,
        slm_camera_model=simulated_camera_model,
        nd_filter_optical_density=5.0,
        quantum_efficiency=0.01,
    )
    camera.set_exposure(100e-6)

    test_image = camera.get_image()

    plt.figure()
    plt.imshow(test_image, cmap="turbo")
    plt.title("Initial Simulated Camera Image")
    plt.colorbar()




.. image-sg:: /auto_examples/calibration/camera_mapping/images/sphx_glr_checkerboard_camera_mapping_001.png
   :alt: Initial Simulated Camera Image
   :srcset: /auto_examples/calibration/camera_mapping/images/sphx_glr_checkerboard_camera_mapping_001.png
   :class: sphx-glr-single-img


.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    <matplotlib.colorbar.Colorbar object at 0x0000020492F5AEC0>



.. GENERATED FROM PYTHON SOURCE LINES 88-96

.. code-block:: Python

    slm_camera_model = SLMFFT(
        input_geometry=slm_geometry,
        virtual_slm=VirtualSLM(phase_scaling=1.0),
        slm_field=PixelwiseSLMField(gaussian_beam),
        focal_length=0.25,
        padded_resolution=(2048, 2048),
    )








.. GENERATED FROM PYTHON SOURCE LINES 97-109

.. code-block:: Python

    camera_mapper = CheckerboardMapper(
        slm=slm,
        camera=camera,
        slm_camera_model=slm_camera_model,
    )

    camera_mapping = camera_mapper.map_camera(
        number_of_squares=(7, 9),
        square_size=16,
        number_of_cg_iterations=50,
    )





.. rst-class:: sphx-glr-script-out

 .. code-block:: none

    Starting vortex annihilation...
    Iteration 1: Detected 0 vortices.
    No vortices detected to remove, stopping.
    Autoexposure: exposure = 1.17e-01 s, image_max = 15.0/255,
    hologradpy/calibration/camera_mapping/checkerboard_mapping/checkerboard_mapper.py:231: UserWarning: Autoexposure did not reach its target: the exposure railed against its bounds (0.0, 1.0). The region peaks at 15 of 255 (5.9%) against a target of 95%, at an exposure of 1.000e+00 s. The frames that follow are exposed as reported here, not as asked for.
    Checkerboard detected.
    Checkerboard detected.




.. GENERATED FROM PYTHON SOURCE LINES 110-114

.. code-block:: Python

    camera_mapping.save(data_path + "camera_mapping.asdf")
    slm_camera_model.save(data_path + "slm_camera_model.pt")
    simulated_camera_model.save(data_path + "simulated_camera_model.pt")








.. GENERATED FROM PYTHON SOURCE LINES 115-148

.. code-block:: Python

    camera_image = camera_mapping.visualization_data.camera_image
    simulated_image = camera_mapping.visualization_data.simulated_image
    slm_phase = gpu_to_numpy(slm_camera_model.virtual_slm.get_phase())

    # TODO: Tidy up plotting
    print("Transformation matrix:")
    print(camera_mapping.transform)
    print("Inverse transformation matrix:")
    print(camera_mapping.inverse_transform)

    plt.figure()
    plt.imshow(camera_image, cmap="turbo")
    plt.plot(
        camera_mapping.detected_points[:, 0],
        camera_mapping.detected_points[:, 1],
        "wx",
        label="detected corners",
    )
    plt.plot(
        *camera_mapping.zeroth_order_xy,
        "r+",
        label="zeroth order position",
    )
    plt.plot(
        camera.shape[1] // 2,
        camera.shape[0] // 2,
        "w*",
        label="camera sensor center",
    )
    plt.legend()

    plt.title("Camera Image with Detected Corners")




.. image-sg:: /auto_examples/calibration/camera_mapping/images/sphx_glr_checkerboard_camera_mapping_002.png
   :alt: Camera Image with Detected Corners
   :srcset: /auto_examples/calibration/camera_mapping/images/sphx_glr_checkerboard_camera_mapping_002.png
   :class: sphx-glr-single-img


.. rst-class:: sphx-glr-script-out

 .. code-block:: none

    Transformation matrix:
    [[ 6.10418755e-01  5.18287833e-06  5.84986065e+02]
     [-8.79778903e-06  6.09901607e-01  7.31353433e+02]]
    Inverse transformation matrix:
    [[ 1.63821965e+00 -1.39214146e-05 -9.58325485e+02]
     [ 2.36312066e-05  1.63960873e+00 -1.19914730e+03]]

    Text(0.5, 1.0, 'Camera Image with Detected Corners')



.. GENERATED FROM PYTHON SOURCE LINES 149-156

.. code-block:: Python

    plt.figure()
    plt.imshow(simulated_image, cmap="turbo")
    plt.plot(
        camera_mapping.calculated_points[:, 0], camera_mapping.calculated_points[:, 1], "wx"
    )
    plt.title("Simulated Camera Image with Detected Corners")




.. image-sg:: /auto_examples/calibration/camera_mapping/images/sphx_glr_checkerboard_camera_mapping_003.png
   :alt: Simulated Camera Image with Detected Corners
   :srcset: /auto_examples/calibration/camera_mapping/images/sphx_glr_checkerboard_camera_mapping_003.png
   :class: sphx-glr-single-img


.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    Text(0.5, 1.0, 'Simulated Camera Image with Detected Corners')




.. rst-class:: sphx-glr-timing

   **Total running time of the script:** (0 minutes 12.572 seconds)


.. _sphx_glr_download_auto_examples_calibration_camera_mapping_checkerboard_camera_mapping.py:

.. only:: html

  .. container:: sphx-glr-footer sphx-glr-footer-example

    .. container:: sphx-glr-download sphx-glr-download-jupyter

      :download:`Download Jupyter notebook: checkerboard_camera_mapping.ipynb <checkerboard_camera_mapping.ipynb>`

    .. container:: sphx-glr-download sphx-glr-download-python

      :download:`Download Python source code: checkerboard_camera_mapping.py <checkerboard_camera_mapping.py>`

    .. container:: sphx-glr-download sphx-glr-download-zip

      :download:`Download zipped: checkerboard_camera_mapping.zip <checkerboard_camera_mapping.zip>`


.. only:: html

 .. rst-class:: sphx-glr-signature

    `Gallery generated by Sphinx-Gallery <https://sphinx-gallery.github.io>`_
