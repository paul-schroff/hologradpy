[pytest]
# Without this a bare `pytest` also walks examples/ and docs/, where the example
# scripts execute on import and the gallery output is not a test suite.
testpaths = tests

# Keep the run from descending into generated trees when a path is given explicitly.
norecursedirs = .git __pycache__ docs/_build docs/auto_examples examples/data *.egg-info
